class Solution {
  public:
    int findPosition(int N , int M , int K) {
        // code 
        if(N==1) return 1;
        int pos=M%N+K-1;
       if(pos==N)
           return pos;
       else
           return pos%N;
    }
};
